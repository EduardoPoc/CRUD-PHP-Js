# CRUD-PHP-Js

A Project using PHP + Laravel | JS

## Running the project

Open a terminal in the project root and run:

```bash
cd frontend/

npm install
npm run build

cd ..

docker compose up -d --build
```
